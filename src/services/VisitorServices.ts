export async function trackVisit(idempotencyKey: string) { const response = await fetch('/api/visits', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ idempotencyKey, noticeVersion: 'v1' }), keepalive: true }); if (!response.ok && response.status !== 404) throw new Error('visit'); }
export async function unlockLocations(password: string) { const response = await fetch('/api/admin/unlock', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ scope: 'locations:read', password }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error || 'unlock'); return data; }
export async function getVisitStats(from: string, to: string) { const response = await fetch(`/api/admin/visit-stats?from=${encodeURIComponent(from)}&to=${encodeURIComponent(to)}`, { cache: 'no-store' }); const data = await response.json(); if (!response.ok) throw new Error(data.error || 'stats'); return data; }
export async function getVisits(from: string, to: string, page = 1) { const response = await fetch(`/api/admin/visits?from=${encodeURIComponent(from)}&to=${encodeURIComponent(to)}&page=${page}`, { cache: 'no-store' }); const data = await response.json(); if (!response.ok) throw new Error(data.error || 'visits'); return data; }
export async function lockLocations() { await fetch('/api/admin/lock', { method: 'POST' }); }
