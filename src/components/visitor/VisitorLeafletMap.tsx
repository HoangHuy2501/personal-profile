'use client';
import { MapContainer, TileLayer, CircleMarker, Popup } from 'react-leaflet';
export default function VisitorLeafletMap({ points }: { points: { alias: string; latitude: number; longitude: number; city?: string | null; region?: string | null; country?: string | null; createdAt: string }[] }) { const center: [number, number] = points[0] ? [points[0].latitude, points[0].longitude] : [16.0544, 108.2022]; return <div className="visitor-map-canvas"><MapContainer center={center} zoom={points.length ? 4 : 5} scrollWheelZoom={false} style={{ height: '100%', width: '100%' }}><TileLayer attribution="&copy; OpenStreetMap contributors" url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png" />{points.map((point) => <CircleMarker key={`${point.alias}-${point.createdAt}`} center={[point.latitude, point.longitude]} radius={7} pathOptions={{ color: '#ef4444', fillColor: '#ef4444', fillOpacity: .8 }}><Popup><strong>{point.alias}</strong><br />{[point.city, point.region, point.country].filter(Boolean).join(', ') || 'Unknown'}<br />{new Date(point.createdAt).toLocaleString()}</Popup></CircleMarker>)}</MapContainer></div>; }
